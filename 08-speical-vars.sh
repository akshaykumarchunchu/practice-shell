#!/bin/bash

$@: is the symbol of all variables to print all variables we should give 01-variables.sh
$#: is the number of variables we passed
$0: to know the script name
PWD: Caps is the present working directory
$HOEM: home directory of current directory
$USER: which user is running script
"process id of the current shell script: $$"
$!: TO check the process ID of last background command
id -u: is normal user
id 0: is root user